import React from 'react';
import ReactDOM from 'react-dom/client';
import LinearRegressionAnimation from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <LinearRegressionAnimation />
  </React.StrictMode>
);